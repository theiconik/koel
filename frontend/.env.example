NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Omit or set false for real API (default). Use true only for local fixtures without backend.
NEXT_PUBLIC_USE_MOCK=false
# NEXT_PUBLIC_SENTRY_DSN=
# ElevenLabs agent/session secrets stay in backend/.env for signed sessions.
